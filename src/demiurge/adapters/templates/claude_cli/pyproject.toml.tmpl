[project]
name = "$archon_id-archon"
version = "$archon_version"
description = "A Demiurge-minted Archon: $archon_name"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk[http-server]>=1.1",
    "pyyaml>=6.0",
    "uvicorn>=0.30",
]
