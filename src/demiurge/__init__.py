"""Demiurge — a meta-agent that mints, delegates to, and curates Archon agents.

Demiurge owns exactly one loop: mint (spec -> scaffold -> deploy), delegate
(over A2A), and curate (eval-gated admission, outcome-based tenure, field
failures distilled into new eval cases). It rides existing standards —
Agent Format for specs, A2A for delegation, MCP for tools — and owns no
runtime, format, or protocol of its own.
"""

__version__ = "0.1.0"
